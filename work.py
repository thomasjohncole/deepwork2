from app import app
# imports the app variable, from the app package
# the app variable is actually an instance of the Flask class
# which is imported from the flask module, which is inside the venv directory